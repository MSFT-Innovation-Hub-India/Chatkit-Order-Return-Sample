# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2025-01-01-preview
# Vector Store Configuration - This is tied to the AZURE_OPENAI_ENDPOINT above
POLICY_DOCS_VECTOR_STORE_ID=vs_a4SsvuG4mWBbjXuibk2AUstB

# ChatKit Domain Key (for production deployment)
# This is a BUILD-TIME variable, not runtime. For Docker builds, pass it as:
#   docker build --build-arg VITE_CHATKIT_DOMAIN_KEY=domain_pk_... .
#   az acr build --build-arg VITE_CHATKIT_DOMAIN_KEY=domain_pk_... .
# Register your domain at: https://platform.openai.com/settings/organization/security/copilot
# For local development, this is not needed (localhost is auto-allowed)
# VITE_CHATKIT_DOMAIN_KEY=domain_pk_your_key_here

# Azure Cosmos DB Configuration
COSMOS_ENDPOINT=https://your-cosmosdb.documents.azure.com:443/
COSMOS_DATABASE=your-database-name

# Application Configuration
APP_HOST=0.0.0.0
APP_PORT=8000

# Logging
LOG_LEVEL=INFO

# Branding Configuration (Optional)
# Customize these to match your organization's brand
BRAND_NAME=Zava Returns
BRAND_TAGLINE=Zava AI-Powered Returns Assistant
BRAND_LOGO_URL=/static/logo.svg
BRAND_PRIMARY_COLOR=#6366f1
BRAND_FAVICON_URL=/static/favicon.svg
